fullName = "tony stark";  // no need to define type it is dynamically typed 
fullname = " nikki"     // case sensitive 
price = 99.99;
isFollow = true;   // boolean type value
console.log(price);
console.log(fullname);

var age = 25;   // by using var we can redcalere variable many times
var age = 29;

// let age =30;  // cannot be redeclare but can be updated
 age = 80;

 const pi = 3.14;  // cannot be updated or redclare 
//  const a;  //undefined


//***DATA TYPES */

const student = {  // created student object
    fullName:"rahul",
    age:20,
    cgpa:8.2,
    ispass:true,
};

student["fullName"] = "raj";   // we can update the const object key not const
 console.log(student.fullName)
